documentclass: article
header-includes: |
    \pagestyle{empty}
    \usepackage{fontspec}
    \usepackage{polyglossia}
    \usepackage{unicode-math}
    \setmainlanguage{russian}
    \setotherlanguage{english}
    \usepackage{unicode-math}
    \usepackage{caption}
    \captionsetup[figure]{justification=centering}
    \captionsetup[figure]{font={normalfont, it},format=plain,labelsep=period}
    \captionsetup[table]{font={normalfont},format=plain,labelsep=none}
    \usepackage{hyperref}